<h1>🍿🎥 'Sup. Let's Cast this shiz. <i><%= @movie.title %></i> 🎬🧟‍♀️</h1>

<%= form_with url: cast_movie_path, local: true do |f| %>
  <% @roles.each_with_index do |role, i| %>
    <%= f.label role.name %>
    <%= collection_select :role, "actor_id[#{i}]", @actors, :id, :name, prompt: true %>
    <br>
  <% end %>
  
  <%= f.submit "Cast this movie." %>
<% end %>
